export interface WaitListProps {
  // Define your page's prop types here
}

export type FormState = {
  firstName: string;
  lastName: string;
  email: string;
  mobile: string;
  query: string;
  note: string;
};
