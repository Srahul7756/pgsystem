export interface AvatarProps {
  img?: ImageSourcePropType;
  placeholder?: string;
  width: number;
  height: number;
  roundedImage?: boolean;
  roundedPlaceholder?: boolean;
  style?: any; // You can define a specific style type if needed
}
